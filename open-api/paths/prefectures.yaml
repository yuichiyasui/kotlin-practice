get:
  summary: List all prefectures
  tags:
    - prefectures
  operationId: getPrefectures
  responses:
    '200':
      description: all prefectures
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../components/schemas/Prefecture.yaml"